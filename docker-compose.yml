services:
  bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./tmp:/app/tmp
    ports:
      - "${WEBAPP_PORT:-8080}:${WEBAPP_PORT:-8080}"
